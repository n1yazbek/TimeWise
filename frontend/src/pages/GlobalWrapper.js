// import Pomodoro from "../components/Pomodoro";

// const GlobalWrapper = () => {
//   const [started, setStarted] = useState(false);
//   const [isActive, setIsActive] = useState(false);
//   const [endTime, setEndTime] = useState(null);
//   const [timeLeft, setTimeLeft] = useState(initialMinutes * MINUTE);
//   const [initialTime, setInitialTime] = useState(initialMinutes * MINUTE);
//   return (
//     <div>
//       {/* Render the music player and timer */}
//       <MusicPlayer isPlaying={isMusicPlaying} />
//       <Pomodoro
        // started={started}
        // setStarted={setStarted}
        // isActive={isActive}
        // setIsActive={setIsActive}
        // endTime={endTime}
        // setEndTime={setEndTime}
        // timeLeft={timeLeft}
        // setTimeLeft={setTimeLeft}
        // initialTime={initialTime}
        // setInitialTime={setInitialTime}
//       />

//       {/* Pass control methods as props to the child components */}
//       <HomePage playMusic={playMusic} stopMusic={stopMusic} />
//       <NotePage startTimer={startTimer} resetTimer={resetTimer} />
//     </div>
//   );
// };
